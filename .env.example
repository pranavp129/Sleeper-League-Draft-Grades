# Anthropic API key used to generate the per-team recap paragraphs
ANTHROPIC_API_KEY=

# Sleeper league ID (numeric string). Convenience default so it doesn't need
# to be passed via --league-id on every run.
SLEEPER_LEAGUE_ID=
